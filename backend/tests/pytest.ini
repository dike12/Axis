# backend/tests/pytest.ini
[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Add backend/ (parent of tests/) to sys.path so that
# `from core.config import ...` and `from modules.auth...` resolve correctly
# regardless of which directory you invoke pytest from.
pythonpath = ..